<p class="lead">Búsquedas relacionadas con <span class="font-weight-bold"><%= @query %></span></p>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Texto</th>
      <th scope="col">Hits</th>
      <th scope="col">Ultima busqueda</th>
      <th scope="col">Score</th>
    </tr>
  </thead>
  <tbody>
    <% @top_ten_suggestions.each do |suggestion| %>
      <tr 
        <% if @query == suggestion.text %>
            class="bg-success"  
          <% end %> >
          <th scope="row"><%= suggestion.text %></th>
          <td><%= suggestion.hits %></td>
          <td><%= suggestion.elapsed_days %> dias atras</td>
          <td><%= suggestion.score.round(4) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
